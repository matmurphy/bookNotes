<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="./styles/main.css" rel="stylesheet">
    <title>Switch User</title>
</head>
<body>

    <div class="container-fluid col-12 d-flex flex-column justify-content-center select-users">
        <div class="col-11 col-md-10 col-lg-8 col-xl-6 d-flex justify-content-end">
            <img class="add-user" src="./assets/person-plus.svg" />
        </div>
            
        <% if (locals.users) { %> 
            <% for (i=0; i<users.length; i++) { %>
                <div class="col-6 col-md-10 col-lg-8 col-xl-6 mx-auto d-flex justify-content-start align-items-center my-3 user-profile">
                    <div class="col-1 d-flex justify-content-center align-items-center user-icon">
                        <p class="mb-0">
                            <%=users[i].first_name.substr(0,1)%>
                        </p>
                    </div>
                    <div class="col-9 d-flex justify-content-start align-items-center h-100">
                        <p class="mb-0 mx-5">
                            <%=users[i].first_name%> <%=users[i].last_name%> 
                        </p>
                    </div>
                </div>

            <% } %>
        
        
        <% } %>
    </div>

</body>
</html>